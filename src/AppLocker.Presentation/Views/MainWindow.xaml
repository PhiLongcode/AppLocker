<Window x:Class="AppLocker.Presentation.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🔒 AppLocker" Height="580" Width="960"
        MinWidth="720" MinHeight="480"
        Background="{StaticResource PrimaryBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        FontFamily="Segoe UI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="48"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🔒" FontSize="22" VerticalAlignment="Center"/>
                    <TextBlock Text=" AppLocker" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    <Border Background="{StaticResource AccentBrush}" CornerRadius="4"
                            Margin="12,0,0,0" Padding="8,2" VerticalAlignment="Center">
                        <TextBlock Text="v1.0 MVP" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>

                <!-- Buttons -->
                <Button Grid.Column="1" Content="⚙ Cài đặt Rule"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,10,0"
                        Command="{Binding OpenSettingsCommand}"/>

                <Button Grid.Column="2" Content="{Binding MonitorButtonText}"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding ToggleMonitorCommand}"/>
            </Grid>
        </Border>

        <!-- Content: Rule (SQLite) | Phần mềm đã cài (Registry) -->
        <Grid Grid.Row="1" Margin="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="RULE ĐÃ CẤU HÌNH (lưu SQLite)"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12" FontWeight="SemiBold"
                       Margin="0,0,8,10"/>

            <Grid Grid.Row="0" Grid.Column="1" Margin="8,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="PHẦN MỀM ĐÃ CÀI (Registry — tham khảo Process)"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"/>
                <Button Grid.Column="1" Content="📥 Tải danh sách"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding LoadInstalledProgramsCommand}"
                        Padding="12,6"/>
            </Grid>

            <!-- Rules ListView -->
            <Border Grid.Row="1" Grid.Column="0" Margin="0,0,8,0"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="10" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1">
                <ListView x:Name="RulesList"
                          ItemsSource="{Binding Rules}"
                          SelectedItem="{Binding SelectedRule}"
                          ItemContainerStyle="{StaticResource DarkListViewItem}"
                          Padding="0">
                    <ListView.View>
                        <GridView AllowsColumnReorder="True">
                            <GridViewColumn Header="📦 Process Name" Width="220"
                                            DisplayMemberBinding="{Binding ProcessName}"/>
                            <GridViewColumn Header="⚙ Rule Type" Width="120"
                                            DisplayMemberBinding="{Binding RuleType}"/>
                            <GridViewColumn Header="⏱ Mô tả" Width="220"
                                            DisplayMemberBinding="{Binding RuleDescription}"/>
                            <GridViewColumn Header="Trạng thái" Width="120"
                                            DisplayMemberBinding="{Binding StatusText}"/>
                        </GridView>
                    </ListView.View>
                    <ListView.Style>
                        <Style TargetType="ListView" BasedOn="{StaticResource DarkListView}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Rules.Count}" Value="0">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock Text="📋" FontSize="40" HorizontalAlignment="Center"/>
                                                    <TextBlock Text="Chưa có rule nào. Mở Cài đặt để thêm!" 
                                                               Foreground="{StaticResource TextSecondaryBrush}"
                                                               FontSize="13" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                                </StackPanel>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.Style>
                </ListView>
            </Border>

            <!-- Installed programs (reference for rule Process name) -->
            <Border Grid.Row="1" Grid.Column="1" Margin="8,0,0,0"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="10" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1">
                <ListView ItemsSource="{Binding InstalledPrograms}"
                          Style="{StaticResource DarkListView}"
                          ItemContainerStyle="{StaticResource DarkListViewItem}"
                          Padding="0">
                    <ListView.View>
                        <GridView AllowsColumnReorder="True">
                            <GridViewColumn Header="Tên hiển thị" Width="220"
                                            DisplayMemberBinding="{Binding DisplayName}"/>
                            <GridViewColumn Header="Process (rule)" Width="110"
                                            DisplayMemberBinding="{Binding ProcessName}"/>
                            <GridViewColumn Header="Nhà phát hành" Width="120"
                                            DisplayMemberBinding="{Binding Publisher}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="24,0">
                <TextBlock Text="{Binding StatusText}"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
